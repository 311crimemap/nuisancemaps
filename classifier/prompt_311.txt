Your job is to label a list of examples with the most appropriate ordinal index from the list of categories below. For context, the examples are names of 311 requests that need to be labeled into broader categories. Process every input, do not shorten the result set. Format the labeled response as a raw json object (no markdown prefix) with key 'examples', with 'text' and 'index' keys from the categories below:
