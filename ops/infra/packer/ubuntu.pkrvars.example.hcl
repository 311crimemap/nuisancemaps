#
# template: fill out and create ubuntu.pkrvars.hcl
#

ssh_username = ""                # login username

ssh_keys = ["my-identifer"]      # ssh key identifiers existing in hetzner
                                 # console (specific to hetzner) that are added
                                 # to image
