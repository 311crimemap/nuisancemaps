apiVersion: v1
kind: Service
metadata:
  name: db
  annotations:
    description: "alias service for postgresql-ha pgpool service."
  # namespace: <add later>
spec:
  selector:
    app.kubernetes.io/component: pgpool
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
