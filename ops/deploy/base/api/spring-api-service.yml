apiVersion: v1
kind: Service
metadata:
  name: spring-api-service
  namespace: crimemap
  labels:
    app: spring-api-service
spec:
  selector:
    app: spring-api
  ports:
    - name: "http"
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
